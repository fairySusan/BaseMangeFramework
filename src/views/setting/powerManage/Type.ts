export const powerFormRule = {
  name: [{required: true, message: '请输入权限名称', trigger: 'blur'}],
  buttonKey: [{required: true, message: '请输入权限key值', trigger: 'blur'}],
  functions: [{required: true, message: '请选择function', trigger: 'blur'}],
}